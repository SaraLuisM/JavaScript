/* shift() remove o primeiro elementos de um array e retorna esse elemento removido.
Altera o comprimento do array. pop()ultimo elemento; shift()primeiro elemento. */

const numeros = [1, 2, 3];
const primeiroNumero = numeros.shift();
console.log(numeros);
console.log(primeiroNumero);